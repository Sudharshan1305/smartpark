apiVersion: v1
kind: Service

metadata:
  name: smartpark-service
  labels:
    app: smartpark

spec:
  type: NodePort

  selector:
    app: smartpark

  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 5000
      nodePort: 30080